# Shared packages

Reserved for code shared between `apps/action/`, `apps/orchestrator/`, and `apps/dashboard/`. Empty until extraction begins.

Planned packages:

- `agents/` — agent loop, prompts, agent contracts
- `github/` — Octokit wrappers
- `supabase/` — generated DB types and client helpers
- `shared/` — utilities (cost calc, sticky markers, budget, retry)
