# Supabase

Reserved for the database that backs the orchestrator and dashboard.

Will hold:

- `migrations/` — schema for runs, agents, agent_steps, issues, repos, costs
- `seed.sql` — local development seed

Empty until the orchestrator work begins.
