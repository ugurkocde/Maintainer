{
  "name": "maintainer",
  "version": "0.0.1",
  "private": true,
  "description": "Automatic maintenance for GitHub repositories. Triages issues, drafts fix PRs, and orchestrates a fleet of specialized agents.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @maintainer/action",
    "typecheck": "npm run typecheck -w @maintainer/action",
    "test": "npm run test -w @maintainer/action",
    "lint": "npm run lint -w @maintainer/action",
    "all": "npm run typecheck && npm run lint && npm run test && npm run build",
    "format": "prettier --write \"apps/**/src/**/*.ts\""
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
